<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.twoving.dao.IAdminDao">
    <select id="getAdmin" resultType="com.himedia.twoving.dto.AdminVO">
        select * from admins where adminid=#{param1}
    </select>

    <select id="getAllCount" resultType="_int">
        select count(*) from ${param1} where ${param2} like concat('%', #{param3}, '%')
    </select>

    <select id="getProductList" resultType="com.himedia.twoving.dto.ProductVO">
        select * from product where title like concat('%', #{param2}, '%') order by pseq desc limit #{param1.displayRow} offset #{param1.startNum}
    </select>

    <insert id="insertProduct" parameterType="com.himedia.twoving.dto.ProductVO">
        insert into product(kind, genre, mainyn, bestyn, newyn, useyn, title, content, age, count, year, time, image, savefilename, image2, savefilename2)
        values (#{kind}, #{genre}, #{mainyn}, #{bestyn}, #{newyn}, #{useyn}, #{title}, #{content},#{age},#{count},#{year},#{time},#{image},#{savefilename},#{image2},#{savefilename2})
    </insert>

    <select id="getProduct" resultType="com.himedia.twoving.dto.ProductVO">
        select * from product where pseq=#{param1}
    </select>

    <delete id="deleteProduct">
        delete from product where pseq=#{param1}
    </delete>

    <update id="updateProduct" parameterType="com.himedia.twoving.dto.ProductVO">
        update product set kind=#{kind}, genre=#{genre}, bestyn=#{bestyn}, newyn=#{newyn}, useyn=#{useyn},
                           title=#{title}, content=#{content}, age=#{age}, year=#{year},
                           time=#{time}, image=#{image}, savefilename=#{savefilename},
                           image2=#{image2}, savefilename2=#{savefilename2} where pseq=#{pseq}
    </update>

    <select id="getNoticeList" resultType="com.himedia.twoving.dto.NoticeVO">
        select * from notice where title like concat('%', #{param2}, '%') order by noticeyn desc, nseq desc limit #{param1.displayRow} offset #{param1.startNum}
    </select>

    <insert id="insertNotice" parameterType="com.himedia.twoving.dto.NoticeVO">
        insert into notice(userid, title, content, noticeyn)
        values (#{userid}, #{title}, #{content}, #{noticeyn})
    </insert>

    <select id="getNotice" resultType="com.himedia.twoving.dto.NoticeVO">
        select * from notice where nseq=#{param1}
    </select>

    <delete id="deleteNotice">
        delete from notice where nseq=#{param1}
    </delete>

    <update id="updateNotice" parameterType="com.himedia.twoving.dto.NoticeVO">
        update notice set title=#{title}, content=#{content}, noticeyn=#{noticeyn} where nseq=#{nseq}
    </update>

    <select id="getFaqList" resultType="com.himedia.twoving.dto.FaqVO">
        select * from faq where subject like concat('%', #{param2}, '%') order by qseq desc limit #{param1.displayRow} offset #{param1.startNum}
    </select>

    <insert id="insertFaq" parameterType="com.himedia.twoving.dto.FaqVO">
        insert into faq(inquirylist, subject, content)
        values (#{inquirylist}, #{subject}, #{content})
    </insert>

    <select id="getFaq" resultType="com.himedia.twoving.dto.FaqVO">
        select * from faq where qseq=#{param1}
    </select>

    <delete id="deleteFaq">
        delete from faq where qseq=#{param1}
    </delete>

    <update id="updateFaq" parameterType="com.himedia.twoving.dto.FaqVO">
        update faq set inquirylist=#{inquirylist}, subject=#{subject}, content=#{content} where qseq=#{qseq}
    </update>

    <select id="getCustomerList" resultType="com.himedia.twoving.dto.CustomerInquiryVO">
        select * from customerinquiry where inquirytitle like concat('%', #{param2}, '%') order by reply asc, ciseq desc limit #{param1.displayRow} offset #{param1.startNum}
    </select>

    <select id="getCustomerInquiry" resultType="com.himedia.twoving.dto.CustomerInquiryVO">
        select * from customerinquiry where ciseq=#{param1}
    </select>

    <update id="updateInquiryReply" parameterType="com.himedia.twoving.dto.CustomerInquiryVO">
        update customerinquiry set reply=#{reply} where ciseq=#{ciseq}
    </update>

</mapper>
